<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Access Forbidden - SoundScape Hub</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html, body {
            height: 100%;
            font-family: 'Arial', sans-serif;
        }

        body {
            background: linear-gradient(135deg, #e74c3c 0%, #c0392b 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow-x: hidden;
        }

        .error-container {
            width: 100%;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .error-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 50px 40px;
            text-align: center;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            max-width: 500px;
            width: 100%;
            position: relative;
            z-index: 10;
        }

        .error-number {
            font-size: 6rem;
            font-weight: 900;
            color: #e74c3c;
            margin-bottom: 20px;
            text-shadow: 0 0 20px rgba(231, 76, 60, 0.3);
        }

        .error-title {
            font-size: 2rem;
            font-weight: 700;
            color: #2c3e50;
            margin-bottom: 20px;
        }

        .error-subtitle {
            font-size: 1.1rem;
            color: #666;
            margin-bottom: 30px;
            line-height: 1.5;
        }

        .error-icon {
            font-size: 3rem;
            color: #e74c3c;
            margin-bottom: 20px;
            animation: pulse 2s ease-in-out infinite alternate;
        }

        @keyframes pulse {
            from { transform: scale(1); }
            to { transform: scale(1.1); }
        }

        .action-buttons {
            display: flex;
            gap: 15px;
            justify-content: center;
            flex-wrap: wrap;
            margin-top: 30px;
        }

        .btn-error {
            background: linear-gradient(45deg, #e74c3c, #c0392b);
            color: white !important;
            padding: 12px 30px;
            border: none;
            border-radius: 50px;
            text-decoration: none !important;
            font-weight: 600;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            font-size: 0.95rem;
            letter-spacing: 0.3px;
        }

        .btn-error:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(231, 76, 60, 0.4);
            color: white !important;
            text-decoration: none !important;
            background: linear-gradient(45deg, #c0392b, #e74c3c);
        }

        .btn-error:focus {
            outline: none;
            box-shadow: 0 0 0 3px rgba(231, 76, 60, 0.3);
        }

        .btn-error i {
            margin-right: 8px;
        }

        /* Responsive design */
        @media (max-width: 768px) {
            .error-card {
                padding: 40px 20px;
                margin: 10px;
            }

            .error-number {
                font-size: 4rem;
            }

            .error-title {
                font-size: 1.5rem;
            }

            .error-subtitle {
                font-size: 1rem;
            }

            .action-buttons {
                flex-direction: column;
                align-items: center;
            }

            .btn-error {
                width: 100%;
                max-width: 250px;
                justify-content: center;
            }
        }

        /* Floating particles for visual effect */
        .particles {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: 1;
        }

        .particle {
            position: absolute;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            animation: float 6s ease-in-out infinite;
        }

        .particle:nth-child(1) { width: 10px; height: 10px; left: 10%; animation-delay: 0s; }
        .particle:nth-child(2) { width: 15px; height: 15px; left: 30%; animation-delay: 2s; }
        .particle:nth-child(3) { width: 8px; height: 8px; left: 50%; animation-delay: 4s; }
        .particle:nth-child(4) { width: 12px; height: 12px; left: 70%; animation-delay: 1s; }
        .particle:nth-child(5) { width: 20px; height: 20px; left: 90%; animation-delay: 3s; }

        @keyframes float {
            0%, 100% { transform: translateY(100vh) rotate(0deg); opacity: 0; }
            10%, 90% { opacity: 1; }
            50% { transform: translateY(-10vh) rotate(180deg); }
        }
    </style>
</head>
<body>
<!-- Floating particles -->
<div class="particles">
    <div class="particle"></div>
    <div class="particle"></div>
    <div class="particle"></div>
    <div class="particle"></div>
    <div class="particle"></div>
</div>

<div class="error-container">
    <div class="error-card">
        <div class="error-icon">
            <i class="fas fa-ban"></i>
        </div>

        <div class="error-number">403</div>

        <h1 class="error-title">Access Forbidden</h1>

        <p class="error-subtitle">
            You don't have permission to access this VIP area.
            Please check your credentials or contact support.
        </p>

        <div class="action-buttons">
            <a href="{{ path('app_home_page') }}" class="btn-error">
                <i class="fas fa-home"></i>Back to Home
            </a>

            {% if not app.user %}
                <a href="{{ path('app_login') }}" class="btn-error">
                    <i class="fas fa-sign-in-alt"></i>Login
                </a>
            {% endif %}
        </div>
    </div>
</div>

<script>
    // Add smooth entrance animation
    document.addEventListener('DOMContentLoaded', function() {
        const errorCard = document.querySelector('.error-card');
        errorCard.style.opacity = '0';
        errorCard.style.transform = 'translateY(50px)';

        setTimeout(() => {
            errorCard.style.transition = 'all 0.8s cubic-bezier(0.4, 0, 0.2, 1)';
            errorCard.style.opacity = '1';
            errorCard.style.transform = 'translateY(0)';
        }, 100);
    });
</script>
</body>
</html>
